#include <stdio.h>

/**
 *main - Entry point
 *
 *printf - prints to the screen
 *
 * Return: always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
