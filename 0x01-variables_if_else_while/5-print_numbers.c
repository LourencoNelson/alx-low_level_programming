#include <stdio.h>

/**
 *main - Entry point
 *
 *printf - prints to the screen
 *
 * Return: always 0 (Success)
 */
int main () {
	int i;
	
	for(i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}      
	return(0);
}
