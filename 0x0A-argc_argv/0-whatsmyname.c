#include "main.h"

/**
 * main - Entry point
 * @argv: Array of arguments
 * @argc: Size of argv
 *
 * Return: Always (0) on success
 */
int main(int __attribute__((unused)) argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
