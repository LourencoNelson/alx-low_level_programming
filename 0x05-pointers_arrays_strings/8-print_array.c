#include "main.h"

/**
 * print_rev - prints in reverse
 * @s: string
 *
 * putchar - prints to the screen
 *
 */
void print_rev(char *a, int n)
{
	int c = 0;
	int i = 0;
	char ch = *(s + c);

	if (*(s + c) != '\0')
	{
		while (1)
		{
			if (*(s + c) != '\0')
			{
				c++;
			}
			else
			{
				break;
			}
		}
		while (1)
		{
			if (i != n - 1)
			{
				putchar(*(s + i));

				
		}
	}
	putchar('\n');
}
